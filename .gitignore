# Build
node_modules/
.svelte-kit/
build/
dist/

# Local env (NEVER commit)
.env
.env.*
!.env.example

# Local database
*.db
*.db-journal

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Per-machine AI-assistant settings (never shared)
.claude/settings.local.json

# Local audit scratch (reports, draft patches) — deliberately not published
audit/
